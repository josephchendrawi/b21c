<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>@ViewBag.Title</title>
    <meta content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no' name='viewport'>

    <!-- CSS -->
    <link rel="stylesheet" href="@Url.Content("~/Content/main.css")">
    <link rel="stylesheet" href="@Url.Content("~/Content/default.css")">

    <!-- jQuery -->
    <script src="@Url.Content("~/Content/jquery/jQuery-2.1.3.min.js")"></script>
    <!-- Bootstrap -->
    <script src="@Url.Content("~/Content/bootstrap/js/bootstrap.min.js")" type="text/javascript"></script>
    <link href="@Url.Content("~/Content/bootstrap/css/bootstrap.min.css")" rel="stylesheet" type="text/css" />
    <!-- jQuery UI -->
    <script src="@Url.Content("~/Content/jqueryUI/jquery-ui.1.11.2.min.js")"></script>
    <!-- Font Awesome Icons -->
    <link href="@Url.Content("~/Content/font-awesome/css/font-awesome.min.css")" rel="stylesheet" type="text/css" />
    <!-- Ionicons -->
    <link href="@Url.Content("~/Content/ionicons/css/ionicons.min.css")" rel="stylesheet" type="text/css" />
    <!-- AdminLTE -->
    <script src="@Url.Content("~/Content/AdminLTE/js/app.js")" type="text/javascript"></script>
    <link href="@Url.Content("~/Content/AdminLTE/css/AdminLTE.min.css")" rel="stylesheet" type="text/css" />
    <link href="@Url.Content("~/Content/AdminLTE/css/skins/_all-skins.min.css")" rel="stylesheet" type="text/css" />
    <!-- iCheck 1.0.1 -->
    <script src="@Url.Content("~/Content/plugins/iCheck/icheck.min.js")" type="text/javascript"></script>
    <link href="@Url.Content("~/Content/plugins/iCheck/all.css")" rel="stylesheet" type="text/css" />
    <script type="text/javascript">
        //iCheck for checkbox and radio inputs
        $(function () {
            $('input[type="checkbox"].minimal, input[type="radio"].minimal').iCheck({
                checkboxClass: 'icheckbox_minimal-blue',
                radioClass: 'iradio_minimal-blue'
            });
            $('input[type="checkbox"].square, input[type="radio"].square').iCheck({
                checkboxClass: 'icheckbox_square-blue',
                radioClass: 'iradio_square-blue'
            });
        });
    </script>
    <!-- SlimScroll -->
    <script src="@Url.Content("~/Content/plugins/slimScroll/jquery.slimscroll.min.js")" type="text/javascript"></script>
    <!-- FastClick -->
    <script src='@Url.Content("~/Content/plugins/fastclick/fastclick.min.js")'></script>
    <!-- Toastr -->
    <script src="@Url.Content("~/Content/plugins/toastr/toastr.js")"></script>
    <link rel="stylesheet" href="@Url.Content("~/Content/plugins/toastr/toastr.css")">
    <script type="text/javascript">
        //alert override
        window.alert = function (msg, type) {
            if (type == "success") {
                toastr.success(msg, "Success");
            }
            else if (type == "warning") {
                toastr.warning(msg, "Warning");
            }
            else if (type == "error") {
                toastr.error(msg, "Error");
            }
            else {
                toastr.info(msg, "Info");
            }
        }
    </script>

    <!-- Jquery Validation -->
    @Scripts.Render("~/bundles/jqueryval")
    <script type="text/javascript">
        //tooltip for validation
        $(function () {
            $(document).tooltip({
                items: ".input-validation-error",
                content: function () {
                    var validationtxt = "";
                    if ($(this).val() == "") {
                        validationtxt = $(this).attr('data-val-required');
                    }
                    else {
                        if ($(this).attr('data-val-length') != undefined) {
                            validationtxt = $(this).attr('data-val-length')
                        }
                        else if ($(this).attr('data-val-equalto') != undefined) {
                            validationtxt = $(this).attr('data-val-equalto')
                        }
                        else if ($(this).attr('data-val-regex') != undefined) {
                            validationtxt = $(this).attr('data-val-regex')
                        }
                    }
                    return validationtxt;
                },
                position: {
                    my: "center bottom-8",
                    at: "center top",
                    using: function (position, feedback) {
                        $(this).css(position);
                        $("<div>")
                            .addClass("arrow")
                            .addClass(feedback.vertical)
                            .addClass(feedback.horizontal)
                            .appendTo(this);
                    }
                }
            });
        });
    </script>
    
    <!-- FancyBox -->
    <link rel="stylesheet" href="@Url.Content("~/Content/plugins/fancybox/jquery.fancybox.css")">
    <script src='@Url.Content("~/Content/plugins/fancybox/jquery.fancybox.js")'></script>
    <script src='@Url.Content("~/Content/plugins/fancybox/jquery.mousewheel-3.0.6.pack.js")'></script>
    <script type="text/javascript">
        $('.fancybox').fancybox({
            afterShow: function () {
                var iviewer = $('.fancybox-inner').iviewer(
                {
                    src: $.fancybox.current.href,
                });
                $('img.fancybox-image').attr('src', '');
            }
        });
    </script>
    <!-- iViewer -->
    <script src="~/Content/iviewer/jquery.iviewer.min.js"></script>
    <link href="~/Content/iviewer/jquery.iviewer.css" rel="stylesheet">

    <!-- Image Orientation -->
    <script type="text/javascript">
        $(function () {
            $('.img-responsive-flex').each(function (i, obj) {
                if ($(obj).height() < $(obj).width()) {
                    $(obj).addClass("img-responsive-flex-landscape");
                }
                else {
                    $(obj).addClass("img-responsive-flex-portrait");
                }
            });
        });
    </script>

    <!-- LabelFor Asterisk for Required field -->
    <script type="text/javascript">
        $(function () {
            $('input').each(function () {
                var req = $(this).attr('data-val-required');
                if (undefined != req) {
                    var label = $('label[for="' + $(this).attr('id') + '"]');
                    var text = label.text();
                    if (text.length > 0) {
                        label.append('<span style="color:red"> *</span>');
                    }
                }
            });
            $('select').each(function () {
                var req = $(this).attr('data-val-required');
                if (undefined != req) {
                    var label = $('label[for="' + $(this).attr('id') + '"]');
                    var text = label.text();
                    if (text.length > 0) {
                        label.append('<span style="color:red"> *</span>');
                    }
                }
            });
        });
    </script>

    <!-- function detect 'touch device' -->
    <script>
        function is_touch_device() {
            return !!('ontouchstart' in window) // works on most browsers 
                || !!('onmsgesturechange' in window); // works on ie10
        };
    </script>
</head>

@{
    string ControllerName = HttpContext.Current.Request.RequestContext.RouteData.Values["controller"].ToString();
    string ActionName = ViewContext.RouteData.Values["action"].ToString();
}
<body class="skin-blue">
    @if (TempData["Message"] != null)
    {
        <script type="text/javascript">
            alert("@Html.Raw(TempData["Message"])");
        </script>
    }
    <div class="wrapper">
        <!-- Main Header -->
        <header class="main-header">
            <!-- Logo -->
            <a href="@Url.Action("Index","Admin")" class="logo">Bags<b>21</b>Century<span class="font10"> ADMIN</span></a>
            <!-- Header Navbar -->
            <nav class="navbar navbar-static-top" role="navigation">
                <!-- Sidebar toggle button-->
                <a href="#" class="sidebar-toggle" data-toggle="offcanvas" role="button">
                    <span class="sr-only">Toggle navigation</span>
                </a>
                <!-- Navbar Right Menu -->
                <div class="navbar-custom-menu">
                    <ul class="nav navbar-nav">
                        <!-- Notifications: style can be found in dropdown.less -->
                        <li class="dropdown notifications-menu">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                <i class="fa fa-usd"></i>
                                <span class="label label-danger lbl-topup-notification-count">0</span>
                            </a>
                            <ul class="dropdown-menu">
                                <li class="header">You have <span class="lbl-topup-notification-count">0</span> pending top up(s)</li>
                                <li>
                                    <!-- inner menu: contains the actual data -->
                                    <ul class="menu" id="topup-notification-menu"></ul>
                                </li>
                                @*<li class="footer"><a href="#">View all</a></li>*@
                            </ul>
                            <script>
                                $(function () {
                                    $.ajax({
                                        url: "@Url.Action("GetTopUpPending", "Admin")",
                                        type: "POST",
                                        dataType: "json",
                                        contentType: "application/json",
                                        success: function (result) {
                                            if (result.Success) {
                                                $('.lbl-topup-notification-count').html(result.Result.TotalCount);

                                                $('#topup-notification-menu').html("");
                                                $.each(result.Result.NotificationList, function (index, value) {
                                                    //console.log(value);
                                                    $('#topup-notification-menu').append('<li><a href="@Url.Action("List", "UserBalanceTopUp")"><i class="fa fa-exclamation-circle text-primary"></i> ' + value.Username + ' - Rp. ' + value.Amount + '<br/>&emsp;&emsp; at ' + value.CreatedAt + '</a></li>');
                                                });
                                            }
                                            else {
                                                alert(result.Message);
                                            }
                                        },
                                        error: function () {
                                            alert("Error!");
                                            //location.reload();
                                        }
                                    });
                                });
                            </script>
                        </li>
                        <!-- Notifications: style can be found in dropdown.less -->
                        <li class="dropdown notifications-menu">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                <i class="fa fa-truck"></i>
                                <span class="label label-danger lbl-paid-order-notification-count">0</span>
                            </a>
                            <ul class="dropdown-menu">
                                <li class="header">You have <span class="lbl-paid-order-notification-count">0</span> paid order(s)</li>
                                <li>
                                    <!-- inner menu: contains the actual data -->
                                    <ul class="menu" id="paid-order-notification-menu">                                        
                                    </ul>
                                </li>
                                @*<li class="footer"><a href="#">View all</a></li>*@
                            </ul>
                            <script>
                                $(function () {                                                        
                                    $.ajax({
                                        url: "@Url.Action("GetBuyer_FullyPaidOrder", "Admin")",
                                        type: "POST",
                                        dataType: "json",
                                        contentType: "application/json",
                                        success: function (result) {
                                            if (result.Success)
                                            {
                                                $('.lbl-paid-order-notification-count').html(result.Result.TotalCount);

                                                $('#paid-order-notification-menu').html("");
                                                $.each(result.Result.NotificationList, function (index, value) {
                                                    //console.log(value);
                                                    $('#paid-order-notification-menu').append('<li><a href="@Url.Action("Shipment", "Order", new { Id = "" })/' + value.Id + '"><i class="fa fa-shopping-cart text-primary"></i> ' + value.OrderCode + ' - ' + value.BookedBy + ', ' + value.ProductCount + ' item(s)</a></li>');
                                                });                                                
                                            }
                                            else
                                            {                                                                   
                                                alert(result.Message);
                                            }
                                        },
                                        error: function () {
                                            alert("Error!");
                                            //location.reload();
                                        }
                                    });
                                });
                            </script>
                        </li>                        
                        <!-- Notifications: style can be found in dropdown.less -->
                        <li class="dropdown notifications-menu">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                <i class="fa fa-bell-o"></i>
                                <span class="label label-danger lbl-neworder-notification-count">0</span>
                            </a>
                            <ul class="dropdown-menu">
                                <li class="header">You have <span class="lbl-neworder-notification-count">0</span> new order(s)</li>
                                <li>
                                    <!-- inner menu: contains the actual data -->
                                    <ul class="menu" id="neworder-notification-menu"></ul>
                                </li>
                                @*<li class="footer"><a href="#">View all</a></li>*@
                            </ul>
                            <script>
                                $(function () {
                                    $.ajax({
                                        url: "@Url.Action("GetBuyer_NewOrder", "Admin")",
                                        type: "POST",
                                        dataType: "json",
                                        contentType: "application/json",
                                        success: function (result) {
                                            if (result.Success)
                                            {
                                                $('.lbl-neworder-notification-count').html(result.Result.TotalCount);

                                                $('#neworder-notification-menu').html("");
                                                $.each(result.Result.NotificationList, function (index, value) {
                                                    //console.log(value);
                                                    $('#neworder-notification-menu').append('<li><a href="@Url.Action("Shipment", "Order", new { Id = "" })/' + value.Id + '"><i class="fa fa-shopping-cart text-primary"></i> ' + value.OrderCode + ' - ' + value.BookedBy + ', ' + value.ProductCount + ' item(s)</a></li>');
                                                });
                                            }
                                            else
                                            {
                                                alert(result.Message);
                                            }
                                        },
                                        error: function () {
                                            alert("Error!");
                                            //location.reload();
                                        }
                                    });
                                });
                            </script>
                        </li>
                        <!-- User Account Menu -->
                        <li class="dropdown user user-menu">
                            <!-- Menu Toggle Button -->
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                <!-- The user image in the navbar-->
                                <img src="@Url.Content("~/Content/AdminLTE/img/user.png")" class="user-image" alt="User Image" />
                                <!-- hidden-xs hides the username on small devices so only the image appears. -->
                                <span class="hidden-xs">@(Session["Name"] ?? "...")</span>
                            </a>
                            <ul class="dropdown-menu">
                                <!-- The user image in the menu -->
                                <li class="user-header" style="height: auto;">
                                    <img src="@Url.Content("~/Content/AdminLTE/img/user.png")" class="img-circle" alt="User Image" />
                                    <p>
                                        @(Session["Name"] ?? "...")
                                        <small>@(Session["Email"] ?? "...")</small>
                                    </p>
                                </li>
                                <!-- Menu Footer-->
                                <li class="user-footer">
                                    <div class="pull-right">
                                        <a href="@Url.Action("LogOut", "Admin")" class="btn btn-default btn-flat">Sign out</a>
                                    </div>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
            </nav>
        </header>
        <!-- Left side column. contains the logo and sidebar -->
        <aside class="main-sidebar">
            <!-- sidebar: style can be found in sidebar.less -->
            <section class="sidebar">
                <!-- search form -->
                <form action="@Url.Action("List", "Order")" method="get" class="sidebar-form">
                    <div class="input-group">
                        @*<input type="hidden" name="TableRefresh" value="true">*@
                        <input type="text" name="OrderCode" class="form-control" placeholder="Search...">
                        <span class="input-group-btn">
                            <button type="submit" name="" id="search-btn" class="btn btn-flat">
                                <i class="fa fa-search"></i>
                            </button>
                        </span>
                    </div>
                </form>
                <!-- /.search form -->
                <!-- sidebar menu: : style can be found in sidebar.less -->
                <ul class="sidebar-menu">
                    <li class="header">MAIN NAVIGATION</li>
                    <li @if (ControllerName.ToLower() == "admin" && ActionName.ToLower() == "index") { @Html.Raw("class='active'")}>
                        <a href="@Url.Action("Index","Admin")">
                            <i class="fa fa-dashboard"></i> <span>Dashboard</span>
                        </a>
                    </li>
                    @if (AccessControl.IsAccessable((long)Session["Id"], AccessModule.Buyer))
                    {
                        <li class="treeview @if (ControllerName.ToLower().Contains("user")) { @Html.Raw("active") }">
                            <a href="#">
                                <i class="fa fa-users"></i> <span>Customer & Reseller</span>
                                <i class="fa fa-angle-left pull-right"></i>
                            </a>
                            <ul class="treeview-menu">
                                <li><a href="@Url.Action("List", "User")"><i class="fa fa-circle-o"></i> Customer & Reseller List</a></li>
                                <li><a href="@Url.Action("List", "UserBalanceTopUp")"><i class="fa fa-circle-o"></i> Balance Top Up</a></li>
                                <li><a href="@Url.Action("List", "UserStatus")"><i class="fa fa-circle-o"></i> User Status</a></li>
                            </ul>
                        </li>
                    }
                    @if (AccessControl.IsAccessable((long)Session["Id"], AccessModule.Product))
                    {
                        <li class="treeview @if (ControllerName.ToLower() == "product" || ControllerName.ToLower() == "productcategory") { @Html.Raw("active") }">
                            <a href="#">
                                <i class="fa fa-briefcase"></i> <span>Product</span>
                                <i class="fa fa-angle-left pull-right"></i>
                            </a>
                            <ul class="treeview-menu">
                                <li><a href="@Url.Action("List", "Product")"><i class="fa fa-circle-o"></i> Product</a></li>
                                @if (AccessControl.IsAccessable((long)Session["Id"], AccessModule.ProductCategory))
                                {
                                    <li><a href="@Url.Action("List", "ProductCategory")"><i class="fa fa-circle-o"></i> Category</a></li>
                                }
                            </ul>
                        </li>
                    }
                    @if (AccessControl.IsAccessable((long)Session["Id"], AccessModule.Order))
                    {
                        <li class="treeview @if (ControllerName.ToLower() == "order") { @Html.Raw("active") }">
                            <a href="#">
                                <i class="fa fa-list-ol"></i> <span>Order</span>
                                <i class="fa fa-angle-left pull-right"></i>
                            </a>
                            <ul class="treeview-menu">
                                <li><a href="@Url.Action("List", "Order")"><i class="fa fa-circle-o"></i> Order</a></li>
                                @if (AccessControl.IsAccessable((long)Session["Id"], AccessModule.MutationCheck))
                                {
                                    <li><a href="@Url.Action("MutationCheck", "Order")"><i class="fa fa-circle-o"></i> Mutation Check</a></li>
                                }
                            </ul>
                        </li>
                    }   
                    @if (AccessControl.IsAccessable((long)Session["Id"], AccessModule.Report))
                    {
                        <li class="treeview @if (ControllerName.ToLower() == "report" || ControllerName.ToLower() == "productstocklog") { @Html.Raw("active") }">
                            <a href="#">
                                <i class="fa fa-line-chart"></i> <span>Report</span>
                                <i class="fa fa-angle-left pull-right"></i>
                            </a>
                            <ul class="treeview-menu">
                                <li><a href="@Url.Action("MonthlyReport", "Report")"><i class="fa fa-circle-o"></i> Monthly Report</a></li>
                                <li><a href="@Url.Action("Index", "ProductStockLog", new { Date = DateTime.Now.ToString("dd-MM-yyyy") })"><i class="fa fa-circle-o"></i> Product Stock Log</a></li>
                                <li><a href="@Url.Action("OrderProductReport", "Report")"><i class="fa fa-circle-o"></i> Order Product Report</a></li>
                                <li><a href="@Url.Action("OrderPackingShift", "Report")"><i class="fa fa-circle-o"></i> Packing Shift</a></li>
                                <li><a href="@Url.Action("ShippingReport", "Report")"><i class="fa fa-circle-o"></i> Shipping Report</a></li>
                                <li><a href="@Url.Action("PartnerOrderReport", "Report")"><i class="fa fa-circle-o"></i> Partner Order Report</a></li>
                            </ul>
                        </li>
                    }
                    @if (AccessControl.IsAccessable((long)Session["Id"], AccessModule.Catalog))
                    {
                        <li @if (ControllerName.ToLower() == "catalog") { @Html.Raw("class='active'")  }>
                            <a href="@Url.Action("List","Catalog")">
                                <i class="fa fa-file-text"></i> <span>Catalog</span>
                            </a>
                        </li>
                    }
                    @if (AccessControl.IsAccessable((long)Session["Id"], AccessModule.Newsletter))
                    { 
                        <li class="treeview @if (ControllerName.ToLower() == "newsletter" || ControllerName.ToLower() == "newslettersubscriber") { @Html.Raw("active") }">
                            <a href="#">
                                <i class="fa fa-envelope"></i> <span>Newsletter</span>
                                <i class="fa fa-angle-left pull-right"></i>
                            </a>
                            <ul class="treeview-menu">
                                <li><a href="@Url.Action("List", "Newsletter", new { Date = DateTime.Now.ToString("dd-MM-yyyy") })"><i class="fa fa-circle-o"></i> Newsletter</a></li>
                                <li><a href="@Url.Action("List", "NewsletterSubscriber")"><i class="fa fa-circle-o"></i> Newsletter Subscriber</a></li>
                            </ul>
                        </li>
                    }
                    @if (AccessControl.IsAccessable((long)Session["Id"], AccessModule.Banner))
                    { 
                        <li @if (ControllerName.ToLower() == "banner") { @Html.Raw("class='active'")   }>
                            <a href="@Url.Action("List", "Banner")">
                                <i class="fa fa-picture-o"></i> <span>Banner</span>
                            </a>
                        </li>
                    }
                    @if (AccessControl.IsAccessable((long)Session["Id"], AccessModule.Survey))
                    {
                        <li @if (ControllerName.ToLower() == "survey") { @Html.Raw("class='active'")    }>
                            <a href="@Url.Action("List", "Survey")">
                                <i class="fa fa-tasks"></i> <span>Survey</span>
                            </a>
                        </li>
                    }
                    @if (AccessControl.IsAccessable((long)Session["Id"], AccessModule.Lookup))
                    {
                        <li @if (ControllerName.ToLower() == "lookup") { @Html.Raw("class='active'")    }>
                            <a href="@Url.Action("List", "Lookup")">
                                <i class="fa fa-clipboard"></i> <span>Lookup</span>
                            </a>
                        </li>
                    }
                    @if (AccessControl.IsAccessable((long)Session["Id"], AccessModule.Admin))
                    { 
                        <li class="treeview @if ((ControllerName.ToLower().Contains("admin") || ControllerName.ToLower().Contains("role"))&& ActionName.ToLower() != "index") { @Html.Raw("active") }">
                            <a href="#">
                                <i class="fa fa-key"></i> <span>Admin</span>
                                <i class="fa fa-angle-left pull-right"></i>
                            </a>
                            <ul class="treeview-menu">
                                <li><a href="@Url.Action("List", "Admin")"><i class="fa fa-circle-o"></i> Admin</a></li>
                                <li><a href="@Url.Action("List", "Role")"><i class="fa fa-circle-o"></i> Role</a></li>
                            </ul>
                        </li>
                    }
                </ul>
            </section>
            <!-- /.sidebar -->
        </aside>
        <!-- Content Wrapper. Contains page content -->
        <div class="content-wrapper">
            <!-- Content Header (Page header) -->
            <section class="content-header">
                <h1>
                    <small>@RenderSection("Title", false)</small>
                    <span class="w200">&nbsp;</span>
                    @RenderSection("ContentHeader", false)
                </h1>
            </section>
            <!-- Main content -->
            <section class="content">
                @RenderBody()
            </section><!-- /.content -->
        </div><!-- /.content-wrapper -->
        <!-- Main Footer -->
        <footer class="main-footer">
            <!-- To the right -->
            <div class="pull-right hidden-xs">
                <!-- Anything you want -->
            </div>
            <!-- Default to the left -->
            <strong>Copyright &copy; 2016 <!-- <a href="#">Company</a>. --></strong> All rights reserved.
        </footer>
    </div><!-- ./wrapper -->

    <!-- Alert Kendo Error Handling-->
    <script type="text/javascript">
        function error_handler(e) {
            alert(e.errorThrown);
        }
    </script>
</body>
</html>